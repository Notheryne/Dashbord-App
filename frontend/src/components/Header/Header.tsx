interface HeaderProps {
}

const Header = ({}: HeaderProps) => {
    return <div className={"flex flex-1 h-20 items-center p-5 dark:bg-stone-900"}>
			Header
		</div>
}

export default Header